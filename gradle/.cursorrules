本项目固定使用：Gradle 9.4.1（见 gradle/wrapper/gradle-wrapper.properties，发行包为国内镜像 all.zip）、Android Gradle Plugin 8.7.2（见 gradle/libs.versions.toml 的 agp）。settings.gradle.kts 已启用 org.gradle.toolchains.foojay-resolver-convention 0.8.0。app 模块使用 Java/Kotlin 目标与工具链 **21**（kotlin.jvmToolchain(21)），Android Studio 中 **Gradle JDK 可直接选本机 jbr-21**，无需再下载 jbr-17。compileSdk / targetSdk 为 **35**（满足 androidx.core 1.15.x）。
